# CaesarCipher
A Python project implementing two classic cryptographic algorithms: the Caesar Cipher and the Vigenère Cipher. Includes a Caesar class with methods to encrypt, decrypt, and perform an attack (brute-force) on ciphertext. Also features Vigenère cipher functions for multi-step encoding and decoding.
